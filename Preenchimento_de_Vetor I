#include <stdio.h>

int main() {
    int i, vetor[10], x;
    scanf("%d", &x);

    for (i = 0; i < 10; i++) {
        vetor[i] = x;
        x *= 2;
        printf("N[%d] = %d\n", i, vetor[i]);
    }

    return 0;
}
